/** Shared focus-visible ring for filled/solid controls (buttons, checkboxes) that need visual separation from their own background. */
export const FOCUS_RING = "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[rgb(var(--primary)/0.5)] focus-visible:ring-offset-2 focus-visible:ring-offset-[rgb(var(--background))]";

/** Shared focus-visible ring for bordered fields (inputs, selects, textareas) already sitting on the page background. */
export const FOCUS_RING_INSET = "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[rgb(var(--primary)/0.35)]";

/** Shared active/pressed feedback for clickable controls. */
export const PRESSABLE = "active:scale-[0.98]";
