"use client"; import { useTranslation } from "@/i18n/useTranslation"; import { Button, Dialog } from "@/shared/ui"; interface Props { isOpen: boolean; onCancel: () => void; onConfirm: () => void; }
export function DeleteModuleDialog({ isOpen, onCancel, onConfirm }: Props) { const t = useTranslation(); return <Dialog isOpen={isOpen} titleId="delete-module-title" onClose={onCancel}><h2 id="delete-module-title" className="text-lg font-semibold text-white">{t("admin.deleteModuleTitle")}</h2><p className="mt-2 text-sm text-zinc-400">{t("common.noUndo")}</p><div className="mt-6 flex justify-end gap-3"><Button variant="ghost" onClick={onCancel}>{t("common.cancel")}</Button><Button variant="danger" onClick={onConfirm}>{t("common.delete")}</Button></div></Dialog>; }
